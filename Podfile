# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FacebookRepositories' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FacebookRepositories
pod 'Alamofire', '~> 5.0'
    pod 'Kingfisher', '~> 5.0'

  target 'FacebookRepositoriesTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
